[tool.pytest.ini_options]
markers = [
    "integration: SuperCollider 実機が起動している状態でのみ実行する統合テスト",
]

[tool.ruff]
line-length = 79

[tool.ruff.lint]
# B101 (assert in non-test code) はテストファイルでは無効化する
per-file-ignores = { "tests/**" = ["S101"] }
